<UserControl x:Class="Desktop.Views.EditWarehouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop.Views" xmlns:viewmodels="clr-namespace:Desktop.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:EditWarehouseViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Edit"></TextBlock>

        <TextBox Width="300"
                     Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     />
        <TextBox Width="300"
                     Text="{Binding Country, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     />
        <TextBox Width="300"
                     Text="{Binding Region, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     />
        <TextBox Width="300"
                     Text="{Binding PostCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     />
        <TextBox Width="300"
                     Text="{Binding City, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     />
        <TextBox Width="300"
                     Text="{Binding Address, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     />

        <StackPanel Orientation="Horizontal">
            <Button Content="Save" Command="{Binding SaveCommand}"></Button>
            <Button Content="Cancel" Command="{Binding CancelCommand}"></Button>
        </StackPanel>
    </StackPanel>
</UserControl>
