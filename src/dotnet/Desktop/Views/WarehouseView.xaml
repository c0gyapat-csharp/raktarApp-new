<UserControl x:Class="Desktop.Views.WarehouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="clr-namespace:Desktop.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" Background="#1e1e1e">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Name}" FontSize="18" Foreground="White" FontWeight="Bold"/>
            <TextBlock FontSize="12" Foreground="Gray">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} {1} {2} {3} {4}">
                        <Binding Path="Country"/>
                        <Binding Path="Region"/>
                        <Binding Path="PostCode"/>
                        <Binding Path="City"/>
                        <Binding Path="Address"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>